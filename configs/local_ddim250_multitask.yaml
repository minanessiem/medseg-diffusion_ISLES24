# Local: DDIM-250 sampler with multi-task loss
# Balanced speed (4x faster) and quality with Dice + BCE auxiliary losses

defaults:
  - local
  - override /diffusion: openai_ddim_250steps
  - override /logging: ddim_250steps
  - override /loss: multitask_equal

