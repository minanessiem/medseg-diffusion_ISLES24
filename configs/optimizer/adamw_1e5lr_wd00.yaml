# AdamW with very low learning rate (1e-5)
# Use for: Multi-GPU training with larger effective batch sizes
#
# Usage: optimizer=adamw_1e5lr_wd00

optimizer_class: adamw
learning_rate: 1.0e-5
betas: [0.9, 0.999]
eps: 1.0e-8
weight_decay: 0.0
amsgrad: false


