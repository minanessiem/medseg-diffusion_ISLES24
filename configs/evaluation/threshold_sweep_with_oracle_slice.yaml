# @package _global_

evaluation:
  levels:
    - slice
  threshold_protocol:
    mode: sweep_with_oracle
    thresholds: "0.05:0.90:0.05"
    fixed_threshold: 0.5
    primary:
      level: slice
      metric: Dice2DForegroundOnly
      statistic: mean
      direction: max
