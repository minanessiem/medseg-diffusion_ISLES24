defaults:
  - openai_base

# OpenAI DDPM with 1000 timesteps, cosine noise schedule
# Logging: 10 snapshots evenly spaced (interval=100)

# Override base settings (base already has timesteps=1000 & cosine schedule)
timesteps: 1000
noise_schedule: cosine
rescale_timesteps: true
sampling_mode: ddpm