defaults:
  - openai_base

# DDIM with 250 sampling steps (4x faster than DDPM)
# Good balance between speed and quality
# Logging: 10 snapshots evenly spaced across 250 respaced steps (interval=25)
timesteps: 1000
noise_schedule: cosine
timestep_respacing: "250"
sampling_mode: ddim
ddim_eta: 0.0

