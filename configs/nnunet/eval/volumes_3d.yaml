# @package _global_
#
# nnU-Net evaluation policy preset for native volume predictions.

defaults:
  - /nnunet/eval: default
  - _self_

nnunet_eval:
  input_format: volumes_3d
  levels:
    - volume
