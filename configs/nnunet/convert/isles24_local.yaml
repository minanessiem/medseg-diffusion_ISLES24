# @package _global_
#
# ISLES24 conversion preset (local environment, baseline modalities).
# Canonical usage:
#   python3 -m scripts.nnunet.convert_to_nnunet \
#       --config-name=nnunet/convert/isles24_local

defaults:
  - local
  - /environment: isles_local
  - /data_profile: isles24_2d_online_baseline
  - _self_
