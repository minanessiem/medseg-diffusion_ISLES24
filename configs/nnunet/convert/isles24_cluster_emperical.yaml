# @package _global_
#
# ISLES24 conversion preset (cluster environment, emperical modalities).
# Canonical usage:
#   python3 -m scripts.nnunet.convert_isles24_2d_dataset_to_nnunet \
#       --config-name=nnunet/convert/isles24_cluster_emperical

defaults:
  - cluster
  - /environment: isles_cluster
  - /data_profile: isles24_2d_online_emperical
  - _self_
