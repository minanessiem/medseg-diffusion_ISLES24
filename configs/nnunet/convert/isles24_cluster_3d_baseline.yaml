# @package _global_
#
# ISLES24 conversion preset (cluster environment, baseline modalities, 3D volumes).
# Canonical usage:
#   python3 -m scripts.nnunet.convert_isles24_2d_dataset_to_nnunet \
#       --config-name=nnunet/convert/isles24_cluster_3d_baseline

defaults:
  - cluster
  - /environment: isles_cluster
  - /data_profile: isles24_3d_fullvol_baseline
  - _self_
