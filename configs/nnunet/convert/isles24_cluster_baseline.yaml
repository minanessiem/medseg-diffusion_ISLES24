# @package _global_
#
# ISLES24 conversion preset (cluster environment, baseline modalities).
# Canonical usage:
#   python3 -m scripts.nnunet.convert_isles24_2d_dataset_to_nnunet \
#       --config-name=nnunet/convert/isles24_cluster_baseline

defaults:
  - cluster
  - /environment: isles_cluster
  - /data_profile: isles24_2d_online_baseline
  - _self_
