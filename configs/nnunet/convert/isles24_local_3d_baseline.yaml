# @package _global_
#
# ISLES24 conversion preset (local environment, baseline modalities, 3D volumes).
# Canonical usage:
#   python3 -m scripts.nnunet.convert_to_nnunet \
#       --config-name=nnunet/convert/isles24_local_3d_baseline

defaults:
  - local
  - /environment: isles_local
  - /data_profile: isles24_3d_fullvol_baseline
  - _self_
