# Wrapper: cluster base + OpenAI DDIM-50 + correct logging
defaults:
  - cluster                     # base cluster environment
  - override /diffusion: openai_ddim_50steps
  - override /logging: ddim_50steps