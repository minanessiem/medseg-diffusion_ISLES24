# DiffSwinTr Large Configuration
# Larger model for potentially better performance
#
# Changes from Base:
# - Larger embed_dim (128 vs 96)
# - Deeper stage 3 (18 vs 6)
# - More attention heads
# - Higher drop_path_rate for regularization
#
# Parameters: ~169.0M

defaults:
  - diffswintr_b

# Override for larger model
embed_dim: 128
depths: [2, 2, 18, 2]
num_heads: [4, 8, 16, 32]
drop_path_rate: 0.2

