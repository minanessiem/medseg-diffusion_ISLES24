defaults:
  - _self_
  - environment: cluster
  - model: unet_cluster
  - dataset: base_isles24
  - training: train_cluster
  - optimizer: default
  - diffusion: default
  - validation: default
  # To include eval params: hydra run +training=eval or compose in config
  - logging: default

random_seed: 42
mode: train  # Or evaluate

hydra:
  run:
    dir: '/mnt/outputs/medsegdiff_outputs/'
