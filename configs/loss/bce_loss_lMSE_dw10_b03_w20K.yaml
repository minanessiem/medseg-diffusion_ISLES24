# BCE-only auxiliary loss (0.3 weight - light)
# Subtle boundary guidance without overwhelming diffusion
# Use when: Testing light supervision on boundaries

defaults:
  - multitask_loss_lMSE_dw10_d01_b01_w20K

# Override: Disable Dice, set BCE to 0.3
auxiliary_losses:
  dice:
    enabled: false
  bce:
    weight: 0.3

