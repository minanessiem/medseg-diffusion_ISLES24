# Dice-only auxiliary loss (0.7 weight)
# Focus on global segmentation overlap
# Use when: Boundary precision less critical than overall coverage

defaults:
  - multitask_loss_lMSE_dw10_d01_b01_w20K

# Override: Increase Dice, disable BCE
auxiliary_losses:
  dice:
    weight: 0.7
  bce:
    enabled: false

