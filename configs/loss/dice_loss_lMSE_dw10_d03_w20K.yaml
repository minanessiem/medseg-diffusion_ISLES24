# Dice-only auxiliary loss (0.3 weight - light)
# Subtle Dice guidance without overwhelming diffusion
# Use when: Testing light supervision on overlap

defaults:
  - multitask_loss_lMSE_dw10_d01_b01_w20K

# Override: Set Dice to 0.3, disable BCE
auxiliary_losses:
  dice:
    weight: 0.3
  bce:
    enabled: false

