# Balanced multi-task loss (0.5 Dice + 0.5 BCE)
# Equal emphasis on global overlap and boundaries
# Standard DiffSwinTr configuration

defaults:
  - multitask_loss_lMSE_dw10_d01_b01_w20K

# Override: Balanced weights
auxiliary_losses:
  dice:
    weight: 0.5
  bce:
    weight: 0.5

