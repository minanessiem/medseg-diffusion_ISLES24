# Light multi-task loss (0.3 Dice + 0.3 BCE)
# Subtle auxiliary supervision, diffusion-dominated
# Use when: Want slight guidance without overwhelming diffusion

defaults:
  - multitask_loss_lMSE_dw10_d01_b01_w20K

# Override: Light weights
auxiliary_losses:
  dice:
    weight: 0.3
  bce:
    weight: 0.3

