# BCE-only auxiliary loss (0.7 weight)
# Focus on pixel-wise boundary refinement
# Use when: Sharp boundaries more important than global overlap

defaults:
  - multitask_loss_lMSE_dw10_d01_b01_w20K

# Override: Disable Dice, increase BCE
auxiliary_losses:
  dice:
    enabled: false
  bce:
    weight: 0.7

